#include <stdio.h>
#include <stdio.h>

char arr[] = { 1, 2, 3, 4, 5, 6, 7, 8, 9 };

int main()
{




	return 0;
}

int tongArr(char addr, int soPhanTu)
{
	int tong;
	char* cArr;
	cArr = addr;

	while(cArr < cArr + sizeof(arr)
	tong += *cArr;
	cArr++;	


	return tong;
}